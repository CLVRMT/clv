CREATE TABLE [dbo].[ASS_CYC_SEQ] (
    [id]          INT NOT NULL,
    [cycle_id]    INT NOT NULL,
    [sequence_id] INT NOT NULL,
    CONSTRAINT [PK_ASS_CYC_SEQ] PRIMARY KEY CLUSTERED ([id] ASC)
);

