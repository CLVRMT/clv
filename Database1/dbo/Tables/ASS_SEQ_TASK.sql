CREATE TABLE [dbo].[ASS_SEQ_TASK] (
    [id]          INT NOT NULL,
    [sequence_id] INT NOT NULL,
    [task_id]     INT NOT NULL,
    CONSTRAINT [PK_ASS_SEQ_TASK] PRIMARY KEY CLUSTERED ([id] ASC)
);

