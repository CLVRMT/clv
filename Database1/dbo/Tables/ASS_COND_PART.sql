CREATE TABLE [dbo].[ASS_COND_PART] (
    [id]                INT NOT NULL,
    [condition_id]      INT NOT NULL,
    [condition_part_id] INT NOT NULL,
    CONSTRAINT [PK_ASS_COND_PART] PRIMARY KEY CLUSTERED ([id] ASC)
);

